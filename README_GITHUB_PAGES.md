# ELTA Track Manager Web V1.0

Subir la carpeta `docs` al repositorio.

Luego en GitHub:
Settings -> Pages -> Deploy from branch -> principal -> /docs -> Save.

Antes de publicar, editar `docs/app.js` y completar `firebaseConfig` con los datos reales de Firebase.
